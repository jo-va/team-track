type Query {
	getUser(id: String!): User
	getAllUsers(groupId: String): [User]

	getGroup(id: String!): Group
	getAllGroups(eventId: String): [Group]

	getEvent(id: String!): Event
	getAllEvents: [Event]
}
