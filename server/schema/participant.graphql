type Participant {
    id: ID!,
    username: String!
    jwt: String!
    group: Group!
    event: Event!
    distance: Float
    latitude: Float
    longitude: Float
    isActive: Boolean
    isOutOfRange: Boolean
}
