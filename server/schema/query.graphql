type Query {
    currentUser: User
    currentParticipant: Participant

    user(id: ID!): User
    users: [User]

    participant(id: ID!): Participant
    participants: [Participant]

    group(id: ID!): Group
    groups: [Group]

    event(id: ID!): Event
    events: [Event]
}
